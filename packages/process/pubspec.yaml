name: summarise_process_plugin
description: A compiled MarkText Plus plugin, built with dart compile exe.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  marktext_plus_plugin_sdk:
    path: ./sdk
